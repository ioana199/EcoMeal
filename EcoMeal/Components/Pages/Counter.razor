@page "/counter"
@using EcoMeal.Controllers
@using EcoMeal.Entities
@rendermode InteractiveServer
@inject BusinessController BusinessController

@if(businesses is null)
{
    <p>Loading...</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Image</th>
                <th>Name</th>
                <th>Description</th>
                <th>Address</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var business in businesses)
            {
                <tr>
                    <img src="@business.ImageUrl" alt="@business.Name" width="60" />
                    <td>@business.Name</td>
                    <td>@business.Description</td>
                    <td>@business.Address</td>
                    <td>@business.BusinessType.Name</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private List<Business>? businesses;

    protected override async Task OnInitializedAsync()
    {
        businesses = (await BusinessController.GetAll()).Value;
    }
}
